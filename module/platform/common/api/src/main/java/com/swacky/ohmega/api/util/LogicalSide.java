package com.swacky.ohmega.api.util;

/**
 * Represents the logical side that something is present on, where {@link #COMMON} is a "both" state
 */
public enum LogicalSide {
    CLIENT,
    COMMON,
    SERVER
}
