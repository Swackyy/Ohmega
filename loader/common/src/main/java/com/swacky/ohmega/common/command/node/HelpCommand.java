package com.swacky.ohmega.common.command.node;

import com.mojang.brigadier.builder.LiteralArgumentBuilder;
import com.swacky.ohmega.api.common.command.node.OhmegaCommandNode;
import net.minecraft.commands.CommandBuildContext;
import net.minecraft.commands.CommandSourceStack;

// todo: write this
public class HelpCommand extends OhmegaCommandNode {
    public static final String ELEMENT_ROOT = "help";

    public HelpCommand(CommandBuildContext context, LiteralArgumentBuilder<CommandSourceStack> builder) {
        // todo: write this
    }
}
